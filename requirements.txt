# Core web framework
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6

# Data validation and settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# CLI and utilities
click>=8.0.0
python-dotenv>=1.0.0

# Logging
structlog>=23.0.0

# File handling
aiofiles>=23.0.0

# Note: Heavy ML packages (torch, faster-whisper, nemo, demucs) are not included
# due to Python 3.13 compatibility issues. These will be installed separately
# when deploying to a server with Python 3.10/3.11 support.

# For production deployment with ML models, uncomment and install:
# torch>=2.0.0
# torchaudio>=2.0.0
# faster-whisper>=0.9.0
# nemo-toolkit[asr]>=1.20.0
# demucs>=4.0.0
# ctc-forced-aligner>=0.1.0
# deepmultilingualpunctuation>=0.1.0
# nltk>=3.8.0
